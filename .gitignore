# Build output
dist/
.astro/

# Dependencies
node_modules/

# Environment
.env
.env.local
.env.*.local

# Editor
.DS_Store
.vscode/settings.json

# Project-level evidence and metadata (root-relative only)
# Uses / prefix so src/data/, src/components/, etc. are NOT excluded
/research/
/runs/
/data/
/.agents/

# Markdown docs at project root only
/*.md
# Exceptions (site root level)
!README.md
!package.json

# Internal links doc (site-specific, not project-level)
/internal-links.md
/url-plan.csv
/keyword-map.csv
/SITE-BRIEF.md
/launch-scope.md